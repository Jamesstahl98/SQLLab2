To Run the project you will need to add first setup the "Bookstore.bak" file in on a server.
Then you'll need to add a user secret file to the project containing the key "ConnectionString". This key should contain the connection string to the server.
Other Nuget packages used in this project are FontAwesome.Sharp, Microsoft.EntityFrameWorkCore.SqlServer and Microsoft.EntityFrameworkCore.Tools.